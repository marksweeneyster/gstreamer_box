# Gstreamer Sandbox
To build these samples pkg-config needs to be installed and know where gstreamer is located on the system. 
Location can be the usual places, or `PKG_CONFIG_PATH` environment variable must be set appropriately.

To run on Windows, the run environment needs to have the PATH set to include the gstreamer ***bin*** folder. 